<% if my_verse.belongs_to_user?(current_user) %>
  <%= button_to "Edit", edit_my_verse_path(my_verse), method: :get %>
  <%= button_to "Delete", my_verse_path(my_verse), method: :delete,
      data: { confirm: "Are you sure you want to delete this MyVerse?" } %>
<% end %>
